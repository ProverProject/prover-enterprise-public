import UIKit

class TargetView: UIImageView {
    /*
    override public func layoutSubviews() {
        super.layoutSubviews()
        
        layer.cornerRadius = 0.5 * frame.width
        layer.borderColor = UIColor(red: 0, green: 0.3, blue: 0, alpha: 1).cgColor
        layer.borderWidth = 1
    }
     */
}
